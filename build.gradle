plugins {
    id 'com.android.application' version '8.6.1' apply false
}

// GitHub/AndroidIDE convenience task: build the debug APK from the root project.
tasks.register("assembleDebug") {
    dependsOn(":app:assembleDebug")
}
